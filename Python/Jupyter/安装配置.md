
```bash
# 安装 需要注意对应版本
pip install jupyter ipython ipykernel

#excalidraw 
jupyter notebook --generate-config 

# 启动
jupyter notebook --allow-root --port 8080 --ip='0.0.0.0' --config={pathToConfig} 
```

